def test_func(x):
    return x + 1


def test_fun(x):
    return x + 2


# Commit 1 defines a function called test_func that takes in a parameter 'x' and returns the value of x plus 1.

# Commit 2 defines a function called test_fun that takes in a parameter 'x' and returns the value of x plus 2.

# The difference between the two commits is that the second commit changes the name of the function from 'test_func' to 'test_fun' and changes the return value from 'x + 1' to 'x + 2'.

# Summarize the above text in one sentence

# Commit 2 changed the name of the function from 'test_func' to 'test_fun' and the return value from 'x + 1' to 'x + 2'.
